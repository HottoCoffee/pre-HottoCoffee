components:
  schemas:
    User:
      type: object
      description: User Schema
      properties:
        id:
          type: integer
        email:
          type: string
        display_name:
          type: string
        role:
          type: string
          enum: [admin, normal, readonly]
      required:
        - message
